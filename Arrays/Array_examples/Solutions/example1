//number which occurs odd number of times in a given arra

#include<stdio.h>

void main() 
{
	int n,i,j,count;
	scanf("%d",&n);
	
	int a[n];
	for (i=0;i<n;i++)
		scanf("%d",&a[i]);

	for (i=0;i<n;i++){
		count=0;
		for (j=0;j<n;j++){
			if (a[j]==a[i])
				count++;
		}
		if (count%2==1)
			printf("%d ",a[i]);
	}
}
